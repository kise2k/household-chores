[project]
name = "household-chores"
version = "0.1.0"
description = "A Django app for managing shared household chores"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2,<5.3",
]
